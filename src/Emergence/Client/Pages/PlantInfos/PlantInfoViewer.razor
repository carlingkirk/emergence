@page "/plantinfo/{Id:int}"
@using Emergence.Client.Components

@inherits PlantInfoComponent

@if (!IsModal)
{
    <button class="btn btn-primary" @onclick="Back">Back</button>
}
@if (IsEditable && PlantInfo.PlantInfoId > 0)
{
    <button class="btn btn-primary" @onclick="() => IsEditing = !IsEditing">@(IsEditing ? "Cancel" : "Edit")</button>
}
@if (IsEditing && PlantInfo != null)
{
    <EditPlantInfo RefreshList="RefreshList" PlantInfo="PlantInfo" @bind-IsEditing="IsEditing" @bind-IsItemLoaded="IsItemLoaded" @bind-List="List" />
}
else if (PlantInfo != null)
{
    <ViewPlantInfo RefreshList="RefreshList" PlantInfo="PlantInfo" @bind-IsEditing="IsEditing" @bind-IsItemLoaded="IsItemLoaded" @bind-List="List" />
}
else
{
    <span>Loading...</span>
}

@code {

}
