@page "/lifeform/{Id:int}"
@using Emergence.Client.Components

@inherits LifeformComponent

@if (Lifeform != null)
{
    <ViewLifeform RefreshList="RefreshList" Lifeform="Lifeform" @bind-IsEditing="IsEditing" @bind-IsItemLoaded="IsItemLoaded" @bind-List="List" />
}
else
{
    <span>Loading...</span>
}

@code {

}
