export interface User {
  id: number;
  displayName: string;
  photoId: number;
  photoThumbnailUri: string;
  visibility: number;
}

export interface UserSummary {
  id: number;
  displayName: string;
  photoId: number;
  photoThumbnailUri: string;
  visibility: number;
}
